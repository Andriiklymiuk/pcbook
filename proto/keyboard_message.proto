syntax = "proto3";

package techschool.pcbook;
option go_package = "/pb";

message Keyboard {
  enum Layout {
    UNKNOWN = 0;
    QWERY = 1;
    QWERTZ = 2;
    AZERTY = 3;
  }

  Layout layout = 1;
  bool backlit = 2;
}